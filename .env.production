# This branch is the public portfolio build. It must never render a real login
# or reach the institutional Supabase instance, even when Render is configured
# as a manually-created Static Site instead of a Blueprint.
VITE_DEMO_MODE=true
